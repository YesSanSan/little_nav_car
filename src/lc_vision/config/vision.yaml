/lc_vision:
  ros__parameters:
    sdk_root: /home/cmls/sanwu/AstraSDK-v2.1.3-Ubuntu-x86_64/AstraSDK-v2.1.3-94bca0f52e-20210608T062039Z-Ubuntu18.04-x86_64

    rgb.enable: true
    rgb.width: 640
    rgb.height: 480
    rgb.capture_fps: 15
    rgb.publish_fps: 6
    rgb.bitrate_kbps: 1200
    rgb.gop_size: 30
    rgb.frame_id: cam_optical_frame

    depth.enable: true
    depth.width: 640
    depth.height: 480
    depth.capture_fps: 15
    depth.publish_fps: 4
    depth.bitrate_kbps: 500
    depth.gop_size: 24
    depth.frame_id: cam_optical_frame
    depth.visualization_min_mm: 500
    depth.visualization_max_mm: 10000
    depth.roi_min_valid_mm: 300
    depth.roi_max_valid_mm: 5000
    depth.invalid_as_black: true
    depth.enable_registration: true
    depth.camera_point.flip_x: false
    depth.estimation.min_valid_pixels: 50
    depth.estimation.histogram_bin_size_mm: 50
    depth.estimation.peak_min_ratio: 0.25
    depth.estimation.peak_min_count: 5
    depth.estimation.min_peak_pixels: 20
    depth.estimation.trim_ratio: 0.1
    depth.estimation.near_field_enable: true
    depth.estimation.near_field_min_history_depth_mm: 450
    depth.estimation.near_field_max_expected_depth_mm: 900
    depth.estimation.near_field_invalid_ratio_threshold: 0.45
    depth.estimation.near_field_depth_jump_mm: 350
    depth.estimation.near_field_nearest_valid_margin_mm: 200
    depth.estimation.near_field_history_timeout_sec: 1.0
    depth.estimation.near_field_min_reliable_frames: 2
    depth.estimation.annotate_depth_on_rgb: true
    depth.estimation.annotate_depth_on_depth: true
    depth.debug_marker.enable: true
    depth.debug_marker.topic: ~/depth_debug_marker
    depth.debug_marker.frame_id: base_link
    depth.debug_marker.z: 0.05
    depth.debug_marker.line_width: 0.02
    depth.debug_marker.alpha: 0.9
    depth.debug_marker.use_detection_label_ns: true
    depth.debug_video.histogram.enable: true
    depth.debug_video.histogram.topic: ~/depth_histogram/video
    depth.debug_video.peak_mask.enable: true
    depth.debug_video.peak_mask.topic: ~/depth_peak_mask/video
    image.rotation_degrees: 90

    detector.enable: true
    detector.framework: auto
    detector.backend: gpu
    detector.model_dir: ""
    detector.input_size: 640
    detector.score_threshold: 0.25
    detector.nms_threshold: 0.45
    detector.fps: 10
    detector.vulkan_device_index: 0
    detector.cpu_num_threads: 2
    detector.log_backend_info: true
    detector.target_classes: ["person"]

    tracking.enable: true
    tracking.initial_center_gate_ratio: 0.2
    tracking.max_lost_frames: 5
    tracking.map_match.distance_tolerance_m: 0.5
    tracking.map_match.angle_tolerance_rad: 0.8
    tracking.min_iou_for_match: 0.05
    tracking.max_center_distance_px: 120.0
    tracking.max_depth_delta_mm: 800.0
    tracking.lidar.enable: true
    tracking.lidar.scan_topic: /scan_filtered
    tracking.lidar.min_forward_protection_m: 0.500
    tracking.nav_fallback.enable: true
    tracking.nav_fallback.cmd_vel_topic: /cmd_vel_nav
    tracking.nav_fallback.no_cmd_vel_timeout_sec: 1.5
    tracking.nav_goal.enable: false
    tracking.debug.enable_verbose_logs: false
    tracking.debug.max_track_memories: 32
    tracking.debug.track_memory_timeout_sec: 3.0
    tracking.recovery.enable: true
    tracking.recovery.cmd_vel_topic: /cmd_vel_nav
    tracking.recovery.turn_speed_rad_s: 0.8
    tracking.recovery.reacquire_turn_speed_rad_s: 0.3
    tracking.recovery.lost_delay_sec: 0.2
    tracking.recovery.memory_timeout_sec: 5.0
    tracking.recovery.search_timeout_sec: 10.0
    tracking.visual_servo.enable: true
    tracking.visual_servo.engage_distance_m: 1.1
    tracking.visual_servo.lost_target_distance_m: 1.8
    tracking.visual_servo.align_deadband_px: 36.0
    tracking.visual_servo.forward_deadband_px: 20.0
    tracking.visual_servo.align_band_ratio: 0.2
    tracking.visual_servo.turn_gain: 1.7
    tracking.visual_servo.min_turn_speed_rad_s: 0.3
    tracking.visual_servo.max_turn_speed_rad_s: 3.0
    tracking.visual_servo.linear_gain: 0.9
    tracking.visual_servo.min_linear_speed_m_s: 0.15
    tracking.visual_servo.max_linear_speed_m_s: 0.35

    goal.enable: true
    goal.global_frame_id: map
    goal.robot_frame_id: base_link
    goal.costmap_topic: /global_costmap/costmap_raw
    goal.standoff_distance_m: 0.800
    goal.max_target_distance_m: 1.1
    goal.min_stable_frames: 1
    goal.clearance_radius_m: 0.25
    goal.max_cell_cost: 80
    goal.longitudinal_search_step_m: 0.05
    goal.max_backoff_distance_m: 1.5
    goal.lateral_search_step_m: 0.05
    goal.max_lateral_offset_m: 0.4
    goal.publish_debug_topics: true
    goal.debug_marker.enable: true
    goal.debug_marker.topic: ~/target_map_debug_marker
    goal.debug_marker.frame_id: map
    goal.debug_marker.point_scale: 0.18
    goal.debug_marker.text_scale: 0.16
    goal.debug_marker.alpha: 0.9
    goal.debug_marker.z: 0.08
    goal.debug_marker.text_z_offset: 0.18
